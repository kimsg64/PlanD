<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bit5.wherewego.res.ResDAOImp">
	<select id="resAllSelect" resultType="com.bit5.wherewego.res.ResVO">
		select r.r_num, r.userid, r.c_num, to_char(r.resdate,'MM-DD') resdate, c.name 
		from res r join course c on r.c_num=c.c_num order by r_num desc
	</select>
</mapper>